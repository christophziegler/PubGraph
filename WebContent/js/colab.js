var Colab = (function () {
	
	return {
		
		/**
		 * Displays details on the collaborative work conducted by
		 * two or more authors.
		 * @param {Array} names Array of strings that contains the names
		 * of the authors.
		 */
		show: function (names) {
			
		}
		
	};
	
}());